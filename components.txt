NAME: Navbar
CATEGORY: navigation
DEPENDENCIES: lucide-react, framer-motion
CODE:
import React, { useState } from "react";
import Link from "next/link";
import { Menu, X, ArrowRight } from "lucide-react";

export function Navbar() {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <header className="sticky top-0 z-50 w-full border-b border-border/40 bg-background/95 backdrop-blur supports-[backdrop-filter]:bg-background/60">
      <div className="container mx-auto flex h-16 max-w-7xl items-center justify-between px-4 sm:px-6 lg:px-8">
        <div className="flex items-center gap-6">
          <Link href="/" className="flex items-center space-x-2">
            <span className="text-xl font-bold tracking-tight">AgentX</span>
          </Link>
          <nav className="hidden md:flex items-center gap-6 text-sm font-medium text-muted-foreground">
            <Link href="#features" className="transition-colors hover:text-foreground">Features</Link>
            <Link href="#pricing" className="transition-colors hover:text-foreground">Pricing</Link>
            <Link href="#testimonials" className="transition-colors hover:text-foreground">Testimonials</Link>
            <Link href="#faq" className="transition-colors hover:text-foreground">FAQ</Link>
          </nav>
        </div>
        <div className="hidden md:flex items-center gap-4">
          <Link href="/login" className="text-sm font-medium text-muted-foreground hover:text-foreground">Log in</Link>
          <Link href="/signup" className="inline-flex items-center justify-center rounded-lg bg-foreground px-4 py-2 text-sm font-medium text-background transition-colors hover:bg-foreground/90">
            Get Started <ArrowRight className="ml-2 h-4 w-4" />
          </Link>
        </div>
        <button onClick={() => setIsOpen(!isOpen)} className="md:hidden p-2 text-foreground">
          {isOpen ? <X className="h-6 w-6" /> : <Menu className="h-6 w-6" />}
        </button>
      </div>
      {isOpen && (
        <div className="md:hidden border-b border-border bg-background px-4 pt-2 pb-6 space-y-3">
          <Link href="#features" onClick={() => setIsOpen(false)} className="block py-2 text-base font-medium text-muted-foreground">Features</Link>
          <Link href="#pricing" onClick={() => setIsOpen(false)} className="block py-2 text-base font-medium text-muted-foreground">Pricing</Link>
          <Link href="#testimonials" onClick={() => setIsOpen(false)} className="block py-2 text-base font-medium text-muted-foreground">Testimonials</Link>
          <Link href="#faq" onClick={() => setIsOpen(false)} className="block py-2 text-base font-medium text-muted-foreground">FAQ</Link>
          <div className="pt-4 border-t border-border flex flex-col gap-2">
            <Link href="/login" className="w-full text-center py-2 text-sm font-medium">Log in</Link>
            <Link href="/signup" className="w-full text-center py-2 text-sm font-medium bg-foreground text-background rounded-lg">Get Started</Link>
          </div>
        </div>
      )}
    </header>
  );
}

---

NAME: HeroSection
CATEGORY: hero
DEPENDENCIES: lucide-react, framer-motion
CODE:
import React from "react";
import Link from "next/link";
import { ArrowRight, Sparkles, CheckCircle2 } from "lucide-react";

export function HeroSection({
  badge = "Next-Gen Autonomous Agent",
  title = "Build production-ready apps in minutes",
  description = "AgentX handles everything from frontend UI, backend APIs, database schemas, and live cloud deployment directly from your plain English prompt.",
  primaryAction = { text: "Start Building Free", href: "/signup" },
  secondaryAction = { text: "View Documentation", href: "/docs" }
}) {
  return (
    <section className="relative overflow-hidden py-24 md:py-32 bg-background">
      <div className="container mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 text-center">
        <div className="inline-flex items-center gap-2 rounded-full border border-border/80 bg-muted/50 px-4 py-1.5 text-xs font-semibold text-muted-foreground backdrop-blur-sm mb-8">
          <Sparkles className="h-3.5 w-3.5 text-foreground" />
          <span>{badge}</span>
        </div>
        <h1 className="text-4xl font-extrabold tracking-tight sm:text-6xl md:text-7xl text-foreground max-w-4xl mx-auto leading-tight">
          {title}
        </h1>
        <p className="mt-6 text-lg sm:text-xl text-muted-foreground max-w-2xl mx-auto leading-relaxed">
          {description}
        </p>
        <div className="mt-10 flex flex-col sm:flex-row items-center justify-center gap-4">
          <Link href={primaryAction.href} className="w-full sm:w-auto inline-flex items-center justify-center rounded-xl bg-foreground px-8 py-3.5 text-base font-semibold text-background shadow-lg transition-all hover:opacity-90">
            {primaryAction.text} <ArrowRight className="ml-2 h-4 w-4" />
          </Link>
          <Link href={secondaryAction.href} className="w-full sm:w-auto inline-flex items-center justify-center rounded-xl border border-border bg-background px-8 py-3.5 text-base font-semibold text-foreground transition-all hover:bg-muted">
            {secondaryAction.text}
          </Link>
        </div>
        <div className="mt-12 flex flex-wrap items-center justify-center gap-6 text-sm text-muted-foreground">
          <div className="flex items-center gap-2">
            <CheckCircle2 className="h-4 w-4 text-foreground" />
            <span>Full-stack TypeScript</span>
          </div>
          <div className="flex items-center gap-2">
            <CheckCircle2 className="h-4 w-4 text-foreground" />
            <span>Automated Testing</span>
          </div>
          <div className="flex items-center gap-2">
            <CheckCircle2 className="h-4 w-4 text-foreground" />
            <span>1-Click Deployment</span>
          </div>
        </div>
      </div>
    </section>
  );
}

---

NAME: FeatureGrid
CATEGORY: features
DEPENDENCIES: lucide-react
CODE:
import React from "react";
import { Zap, ShieldCheck, Database, Layers, Cloud, Code2 } from "lucide-react";

const features = [
  {
    icon: Zap,
    title: "Autonomous Architecture",
    description: "Generates robust full-stack architectures with modern best practices, clean schemas, and type-safety built-in."
  },
  {
    icon: Database,
    title: "Instant Database Schemas",
    description: "Wires up PostgreSQL, Supabase, SQLite, and MongoDB databases with migrations, seeds, and client adapters."
  },
  {
    icon: ShieldCheck,
    title: "Authentication & Security",
    description: "Integrates enterprise authentication via Clerk, NextAuth, or Supabase Auth with role-based access control."
  },
  {
    icon: Cloud,
    title: "Continuous Deployment",
    description: "Automatic staging and production deployments to Vercel, Netlify, Railway, or Fly.io with SSL and custom domains."
  },
  {
    icon: Layers,
    title: "Rich UI Component Library",
    description: "Modern accessible components with dark mode, fluid micro-interactions, and responsive layout foundations."
  },
  {
    icon: Code2,
    title: "Self-Healing Builds",
    description: "Executes TypeScript verification, linting, and automated browser testing to resolve errors before shipping."
  }
];

export function FeatureGrid() {
  return (
    <section id="features" className="py-24 bg-muted/30 border-y border-border/60">
      <div className="container mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <div className="text-center max-w-3xl mx-auto mb-16">
          <h2 className="text-3xl font-bold tracking-tight sm:text-4xl text-foreground">
            Everything you need to ship faster
          </h2>
          <p className="mt-4 text-base sm:text-lg text-muted-foreground">
            Engineered for developers and teams who value quality, reliability, and speed.
          </p>
        </div>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          {features.map((feature, idx) => {
            const Icon = feature.icon;
            return (
              <div key={idx} className="rounded-2xl border border-border bg-background p-8 transition-all hover:border-foreground/40 hover:shadow-md">
                <div className="inline-flex p-3 rounded-xl bg-muted text-foreground mb-6">
                  <Icon className="h-6 w-6" />
                </div>
                <h3 className="text-xl font-semibold text-foreground mb-2">{feature.title}</h3>
                <p className="text-sm text-muted-foreground leading-relaxed">{feature.description}</p>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
}
